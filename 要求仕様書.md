# 要求仕様書

## 1. プロジェクト概要

### 1.1 システム名

学習管理アプリ

### 1.2 システム概要
学習の記録を残し、集計したデータを分析する。
### 1.3 作成目的 
学習の習慣化や、記録した学習を多面的に分析することで、更なる学習の効率化を目指す。
### 1.4 解決したい課題
自立した学習習慣の取得。独学では得難い達成感の可視化。
### 1.5 利用目的者
資格取得や副業など、毎日の隙間時間で学習を要する人。

---

## 2. 開発背景
### 2.1 現状の問題点
HTTP,CSS,JS,Javaなどいくつかの言語や基礎知識を学習してきたが、一人でプロジェクト作成を完成させた経験がない。
### 2.2 開発理由
本アプリを製作者自身が継続的に利用することで、今後の自己学習を可視化させる。
### 3.3 開発の目的
設計→実装までのプロセスを経たフルスタック実装のポートフォリオを作成する。

---
## 3. システム構成

### 3.1 全体構成
本システムは、フロントエンド・バックエンド・データベースを分離した構成で開発する。
フロントエンドからバックエンドAPIへHTTP通信を行い、バックエンドがデータベースへアクセスする構成とする。
また、認証方式にはJWT認証を採用する。

### 3.2 フロントエンド
- Next.js
- React
- TypeScript
- Tailwind CSS

フロントエンドでは、ユーザー操作画面の表示およびAPI通信を担当する。
Next.jsを利用することで、画面管理・ルーティング・コンポーネント管理を効率化する。
Tailwind CSSを利用し、保守性の高いUI設計を行う。

### 3.3 バックエンド
- Java
- Spring Boot
- Spring Security
- Spring Data JPA

バックエンドでは、認証処理・業務ロジック・データベース操作を担当する。
REST API形式でフロントエンドと通信を行う。
Spring Securityを利用して認証・認可を実装し、JWTを用いたログイン管理を行う。
Spring Data JPAを利用してデータ操作を行う。

### 3.4 データベース
- PostgreSQL
データベースでは、ユーザー情報・学習カテゴリ・学習ログなどを管理する。

### 3.5 使用技術一覧
| 分類      | 使用技術                            |
| ------- | ------------------------------- |
| フロントエンド | Next.js 15 / React / TypeScript |
| CSS     | Tailwind CSS                    |
| バックエンド  | Java 21 / Spring Boot 4         |
| 認証      | Spring Security / JWT           |
| ORM     | Spring Data JPA                 |
| データベース  | PostgreSQL                      |
| ビルドツール  | Gradle Kotlin DSL               |
| バージョン管理 | Git / GitHub                    |


## 4. 機能要件

### 4.1 認証機能
- ユーザー登録機能
- ログイン機能
- ログアウト機能
- JWTを用いた認証機能
- パスワード暗号化機能

### 4.2 ユーザー機能
- ユーザー情報登録機能
- ユーザー情報編集機能
- ユーザー情報削除機能

### 4.3 学習管理機能

- 学習時間計測機能(タイマー)
- 学習時間計測機能(ストップウォッチ)
- 学習時間計測中断機能
- 学習ログ計測停止機能
- 学習カテゴリ管理機能
- 学習メモ入力機能
- 学習ログ登録機能

### 4.4 学習分析機能

- 総学習時間集計機能
- 週別学習頻度表示機能
- 最多学習時間カテゴリ取得機能
- 日別学習時間分析機能
- 週別学習時間分析機能
- 月別学習時間分析機能
- 日別カテゴリ別学習割合グラフ表示機能
- 週別カテゴリ別学習割合グラフ表示機能
- 月別カテゴリ別学習割合グラフ表示機能
- 学習カテゴリ一覧表示機能
- カテゴリ別分析機能
---

## 5. 非機能要件


### 5.1 セキュリティ

- JWT認証を利用して認証情報を管理する。
- パスワードはハッシュ化（暗号化）して保存する。
- 認証済みユーザーのみ学習データへアクセス可能とする。
- 不正なリクエストに対して適切なエラーレスポンスを返却する。
- バリデーションを実装し、不正入力を防止する。
### 5.2 パフォーマンス
- 一般的な操作において、画面表示およびAPIレスポンスを高速に行う。
- 不要なデータ取得を避け、必要なデータのみ取得する。
- データベースアクセス回数を最適化する。
- 学習分析機能において、一般的なデータ量で安定した集計処理を行う。
### 5.3 可用性
- 10名以下の同時接続者数を想定し、メンテナンスを考慮した99.5%を実現する。
### 5.4 拡張性
- 機能追加しやすい構成を採用する。
- フロントエンド・バックエンドを分離した構成とする。
- MVC構成および責務分離を意識した設計を行う。
### 5.5 レスポンシブ対応
- PC・スマートフォンに対応する。
- 画面サイズに応じてレイアウトを調整する。
- モバイル環境でも操作しやすいUIを実装する。
- Tailwind CSSを利用してレスポンシブデザインを実現する。
---

## 6. 画面設計

### 6.1 画面一覧
| 画面ID | 画面名 | 内容 |
| ----- | ----- | ---- |
| 01 | ログイン画面 | 初期画面。メールアドレスとパスワードを入力することでログインまたは新規登録する。 |
| 02 | メニュー画面 | メニュー選択画面。学習を記録するor学習データを確認。 |
| 03 | 学習ログ作成画面 | 学習ログを作成。時間計測(タイマーorストップウォッチ)、カテゴリ選択、メモを入力し保存する。 |
| 04 | 学習データ確認画面 | 学習ログの確認。 カテゴリ別学習時間のグラフ、選択したカテゴリの詳細情報、ユーザーの総学習時間、期間別学習ログの分析が可能|
| 05 | ハンバーガーメニュー | メニュー選択画面。ハンバーガーボタンを押すとモーダルが表示される。ユーザー情報orログアウト |
| 06 | ユーザー情報 | ユーザーネーム、メールアドレス、パスワードの表示と編集画面。ユーザー削除も可能。 |

### 6.2 画面遷移

- 初期アクセス時はログイン画面（01）を表示する。
- ログイン成功後、メニュー画面（02）へ遷移する。
- ログイン画面（01）から新規登録処理を実行可能とする。
- メニュー画面（02）から学習ログ作成画面（03）へ遷移可能とする。
- メニュー画面（02）から学習データ確認画面（04）へ遷移可能とする。
- 全ログイン済み画面では、ハンバーガーボタン押下時にハンバーガーメニュー（05）を表示可能とする。
- ハンバーガーメニュー（05）からユーザー情報画面（06）へ遷移可能とする。
- ユーザー情報画面（06）では、ユーザー情報編集およびユーザー削除を実行可能とする。
- ハンバーガーメニュー（05）からログアウト処理を実行可能とする。
- ログアウト後はログイン画面（01）へ遷移する。
- 未認証ユーザーは、ログイン画面（01）以外へアクセス不可とする。

### 6.3 UI方針
- シンプルで分かりやすいUIを意識する。
- 学習ログや分析データを視覚的に分かりやすく表示する。
- ボタン・入力フォーム・レイアウトに統一感を持たせる。
- 長時間利用でも見やすい配色を行う。
- グラフや統計情報を用いて、視覚的に把握しやすくする。

---

## 7. API設計

### 7.1 API一覧
| No | エンドポイント | メソッド | 認証 | 概要 |
|----|--------------|----------|------|------|
| 1 | /api/auth/login | POST | 不要 | ログイン |
| 2 | /api/user | POST | 不要 | ユーザー新規登録 | 
| 3 | /api/user | GET | 必要 | ユーザー情報取得 |
| 4 | /api/user | PUT | 必要 | ユーザー情報更新 |
| 5 | /api/user | DELETE | 必要 | ユーザー削除 |
| 6 | /api/studyLog | POST | 必要 | 学習ログを保存 |
| 7 | /api/studyLog/date/{yyyy-mm-dd} | GET | 必要 | 学習ログを取得 |
| 8 | /api/category | GET | 必要 | 学習カテゴリを取得 |
| 9 | /api/category | POST | 必要 | 学習カテゴリを保存 |
| 10 | /api/category/{categoryId} | PUT | 必要 | 学習カテゴリを更新 |
| 11 | /api/category/{categoryId} | DELETE | 必要 | 学習カテゴリを削除 |
| 12 | /api/studyLog/analytics | GET | 必要 | 分析情報を取得 |

### 7.2 リクエスト形式

#### /api/auth/login POST ログイン

```json
{
 "email":"exapmle.exapmle.com",
 "password": "password123" 
}
```

#### /api/user POST ユーザー新規登録

```json
{
 "email":"exapmle.exapmle.com",
 "password": "password123" 
}
```

#### /api/user PUT ユーザー情報更新

```json
{
 "name": "山田 太郎",
 "email":"exapmle.exapmle.com",
 "password": "password123" 
}
```

#### /api/studyLog POST 学習ログを保存

```json
{
  "startTime": "yyyy-MM-dd'T'HH:mm",
  "endTime": "yyyy-MM-dd'T'HH:mm",
  "totalStudySeconds": "6000",
  "category": "HTML",
  "memo": "HTMLを学習した"
}
```

#### /api/category POST 学習カテゴリを保存
```json
{
  "name": "HTML",
  "parent?": "IT",
}
```

#### /api/category/{categoryId} PUT 学習カテゴリを更新
```json
{
  "name": "HTML",
}
```

### 7.3 レスポンス形式

#### 成功
```json
{
  "status": "SUCCESS",
  "data": {},
  "message": ""
}
```

#### エラー
```json
{
  "status": "ERROR",
  "data": null,
  "message": "error message"
}
```

---

## 8. データベース設計

### 8.1 テーブル一覧

| テーブル名 | 概要 |
|----------|------|
| users |	ユーザー情報を管理する |
| study_logs | 学習ログを管理する |
| categories | 学習カテゴリを管理する |
### 8.2 ER図

users

↓

study_logs

↑

categories

```text
users (1)
  |
  | user_id
  |
study_logs (N)
  |
  | category_id
  |
categories (1)
```

---

### 8.3 カラム定義

#### 1.1 usersテーブル

| カラム名     | 型           | 必須 | PK | UNIQUE | 説明          |
|------------|--------------|------|----|--------|--------------|
| user_id    | BIGINT       | ○    | ○  | ○      | ユーザーID　   |
| user_name  | VARCHAR(100) | ○    | -  | -      | ユーザー名     |
| email      | VARCHAR(255) | ○    | -  | ○      | メールアドレス  |
| password   | VARCHAR(255) | ○    | -  | -      | パスワード     |
| created_at | VARCHAR(255) | ○    | -  | -      | 作成日時　     |
| status     | BOOLEAN      | ○    | -  | -      | 使用可能判定　 |

#### 1.2 制約

- PRIMARY KEY: user_id
- UNIQUE: email
- NOT NULL: user_id
- NOT NULL: email
- NOT NULL: password



---

#### 2.1 categoriesテーブル


| カラム名             | 型           | 必須 | PK | UNIQUE | 説明          |
|--------------------|--------------|------|----|--------|--------------|
| category_id.       | BIGINT       | ○    | ○  | ○      | カテゴリID    |
| user_id            | BIGINT       | ○    | -  | -      | 所有ユーザーID |
| parent_category_id | BIGINT       | -    | -  | -      | 親カテゴリID  |
| category_name      | VARCHAR(100) | ○    | -  | -      | カテゴリ名    |
| created_at         | TIMESTAMP    | ○    | -  | -      | 作成日時      |
| status             | BOOLEAN      | ○    | -  | -      | 使用可能判定   |

#### 2.2 制約

- PRIMARY KEY: category_id
- FOREIGN KEY: user_id
- UNIQUE: category_id
- NOT NULL: user_id
- NOT NULL: category_name
- NOT NULL: created_at
- NOT NULL: status

---

#### 3.1 study_logsテーブル

| カラム名             | 型           | 必須 | PK | UNIQUE | 説明          |
|--------------------|--------------|------|----|--------|--------------|
| study_log_id       | BIGINT       | ○    | ○  | ○      | 学習ログID    |
| user_id            | BIGINT       | ○    | -  | -      | ユーザーID    |
| category_id        | BIGINT       | ○    | -  | -      | カテゴリID    |
| start_time         | TIMESTAMP    | ○    | -  | -      | 学習開始日時   |
| end_time           | TIMESTAMP    | ○    | -  | -      | 学習終了日時   |
| study_seconds      | INTEGER      | ○    | -  | -      | 学習時間（秒） |
| memo               | TEXT(1000)   | ○    | -  | -      | 学習内容メモ   |
| created_at         | TIMESTAMP    | ○    | -  | -      | 作成日時      |
| updated_at         | TIMESTAMP    | ○    | -  | -      | 更新日時      |

#### 3.2 制約

- PRIMARY KEY: study_log_id
- FOREIGN KEY: user_id
- FOREIGN KEY: category_id 
- UNIQUE: study_log_id 
- NOT NULL: user_id
- NOT NULL: category_id
- NOT NULL: start_time
- NOT NULL: end_time
- NOT NULL: study_seconds
- NOT NULL: memo
- NOT NULL: created_at
- NOT NULL: updated_at

---

### 8.4 リレーション

#### users : study_logs

- 1対多（1:N）
- 1人のユーザーは複数の学習ログを持つことができる。

#### users : categories

- 1対多（1:N）
- 1人のユーザーは複数のカテゴリを持つことができる。

#### categories : study_logs

- 1対多（1:N）
- 1つのカテゴリに複数の学習ログを紐付けることができる。

---

## 9. 認証・認可

### 9.1 JWT認証

- ログイン成功時にJWTを発行する。
- 認証が必要なAPIへアクセスする際はJWTを送信する。
- JWTの有効期限切れ時は再ログインを要求する。
- tokenにはuserIdを格納する。

### 9.2 セッション管理方針

- クライアント側でJWTを管理する。
- サーバー側でセッション情報やログイン状態は保持しない。
- ログアウト時は保持しているJWTを削除する。
- JWTが無効または期限切れの場合は認証エラーを返却する。
- JWTの期限は24時間とする。

---

## 10. バリデーション

### 10.1 入力チェック

#### ユーザー登録
- 新規登録時はユーザー名は登録しない。
- 新規登録時のユーザー名は「no name」とする。
- メールアドレスは入力必須とする。
- メールアドレスはメールアドレス形式を満たすこと。
- パスワードは入力必須とする。
- パスワードは8文字以上とする。

#### ユーザー更新
- ユーザー名の入力は任意とする。
- ユーザー名は100文字以内とする。
- ユーザー名に入力可能な文字は漢字・ひらがな・カタカナ・アルファベット（大文字・小文字）とする。文字の間の空欄は許容する。文字の先頭にある空欄は除外する。危険文字（< > " '  ; & / \）は禁止する。
- メールアドレスの入力は任意とする。
- メールアドレスはメールアドレス形式を満たすこと。
- パスワードの入力は任意とする。
- パスワードは8文字以上とする。

#### ログイン
- メールアドレスは入力必須とする。
- パスワードは入力必須とする。

#### 学習カテゴリ
- カテゴリ名は入力必須とする。
- カテゴリ名は100文字以内とする。

#### 学習ログ
- カテゴリは選択必須とする。
- 学習メモは任意入力とする。
- 学習メモは1000文字以内とする。

### 10.2 業務チェック
#### ユーザー
- メールアドレスは重複禁止とする。
- 論理削除済みのユーザーからの処理は受け付けないものとする。
#### 学習カテゴリ
- 自身を親カテゴリに指定できないものとする。
- 論理削除済みのカテゴリは使用できないものとする。
- 他ユーザーのカテゴリは使用できないものとする。
#### 学習ログ
- 学習終了日時は学習開始日時以降であることとする。
- 論理削除済みのカテゴリは使用できないものとする。
- 他ユーザーの学習ログは参照できないものとする。

### 10.3 エラーメッセージ方針


| エラー内容 | メッセージ |
|----------|-----------|
| 必須入力漏れ | 登録に必要な情報が不足しています。|
| 禁止文字入力 | 使用できない文字が含まれています。 |
| メールアドレス形式不正 | 正しいメールアドレスを入力してください。 |
| パスワード不足 | パスワードは8文字以上で入力してください。 |
| メールアドレス重複 | 既に登録されているメールアドレスです。 |
| 文字数制限エラー | 登録可能な文字数制限を超えています。 |
| 入力エラー | 入力された値が正しくありません。 |
| ログイン失敗 |	メールアドレスまたはパスワードが正しくありません。 |
| 学習カテゴリ名重複 | 既に登録されているカテゴリ名です。 |
| 認証エラー | 認証エラーが発生しました。再度ログインしてください。 |
| ユーザー取得失敗 | ユーザーが見つかりません。 |
| システムエラー | システムエラーが発生しました。 |
| 作成失敗エラー | データの作成に失敗しました。 |
| 更新失敗エラー | データ更新に失敗しました。 |

---

## 11. テスト方針

### 11.1 単体テスト
- Service層は単体テストを行う。
### 11.2 結合テスト
各層の結合テストを行う。
- Service ⇔ Repository
- Repository ⇔ Database
### APIテスト
- ControllerからDBまでの一連の流れを検証する。

テスト観点は正常系、異常系とする。
---

## 12. 開発環境

### 12.1 使用IDE
- Visual Studio Code
### 12.2 Javaバージョン
- Java 21
### 12.3 Spring Bootバージョン
- Spring Boot 4.0.2
### 12.4 DB環境
- PostgreSQL 14
#### 接続情報
- ホスト：localhost
- ポート：5432
- データベース名：owner
#### ORM
- Spring Data JPA
- hibernate

### 12.5 git運用

#### バージョン管理
- Git

#### ブランチ戦略
- main 
  - 本番用
- develop
  - 開発統合用
- feature/*
  - 機能開発用
---

## 13. 備考

### 13.1 制約事項
- データを削除する際は、物理削除しないこととする。
- リフレッシュトークンは実装対象外とする。

### 13.2 注意事項
- パスワードはハッシュ化することとする。
- フロントエンドとバックエンドの両方で入力チェックを実施する。
- 例外はGlobalExceptionHandlerで管理する。
- エラー時はApiResponseDto形式でレスポンスを返却する。
